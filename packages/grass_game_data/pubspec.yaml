name: grass_game_data
description: Data sources and repository implementations for the grass survivor game.
version: 0.0.1
publish_to: 'none'

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  app_core:
    path: ../app_core
  grass_game_domain:
    path: ../grass_game_domain
