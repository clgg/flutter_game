# 割草类小游戏人物、怪兽图片接入规范

> 用途：约束玩家角色、普通怪、Boss 的 spritesheet 交付格式。下次新增角色或怪兽时，先按本文验收素材，再接入代码，避免动画闪烁、方向错误、背景残留和碰撞观感不一致。

> 当前代码实现：Boss 不再接入独立 `bosses/` 图片目录。普通关卡 Boss 默认可以从现有 `guaishou` 怪兽池随机选择一只，并在运行时按 Boss 血量、体型和掉落规则强化；有主题的关卡可以显式指定固定 Boss 外观，例如第一章农场主题最终 Boss 使用半机械鳄鱼。后续新增可作为 Boss 的外观时，按普通怪兽规范加入 `assets/game/grass_game/images/guaishou/`。
>
> 当前代码实现：人物、普通怪、动物小怪和 Boss 的运行时行走图都按固定 `128x128` 单格读取。正式新增素材统一交付 `768x1024 / 6列 x 8行` 透明 PNG；旧 4 方向或黑底大图只能作为转换源，不能直接进 Flutter assets。

## 1. 通用硬性要求

| 项目 | 要求 |
| --- | --- |
| 文件格式 | 游戏内行走动画统一用 PNG spritesheet。头像、预览图可用 WebP/GIF。 |
| 透明背景 | 必须是真透明 RGBA，不要棋盘格背景、白底、截图底、阴影底。 |
| 单帧尺寸 | 运行时统一 `128x128`。高清 `256x256` 源帧只放设计归档，不直接进 Flutter assets。 |
| 帧排列 | 横向放帧，纵向放方向。每行帧数固定。 |
| 主体锚点 | 每一帧人物或怪兽脚底必须在同一条水平线上，身体中心必须基本对齐。 |
| 安全边距 | 主体不要贴边，建议左右至少 24px，上方至少 8px，下方保留 8-18px。 |
| 阴影 | 阴影必须跟随主体在同一帧内，不允许跨出单元格，也不要用大面积半透明模糊阴影。 |
| 风格 | 同一批角色和怪兽视角、描边粗细、明暗方向、饱和度要一致。 |
| 授权 | 来源、作者、授权、是否可商用、是否需署名必须可追溯。 |

最容易返工的问题：

- 把带棋盘格背景的预览图当透明图交付。
- 每帧主体在 `128x128` 格子里的位置不一致，播放时会抖动或闪。
- 不同方向的角色比例不一致，侧面比正面大很多或小很多。
- 把多张动作截图拼成 sheet，但没有统一裁切和脚底锚点。
- 图片里自带枪、火焰、文字、水印，和游戏运行时子弹、特效叠加后冲突。

## 2. 玩家角色规格

当前玩家动画由 `PlayerAnimationSet` 读取，规格如下：

| 项目 | 规格 |
| --- | --- |
| 文件位置 | `assets/game/grass_game/images/player/` |
| 文件名 | `player_<角色id>_walk_8dir_sheet.png` |
| 图片尺寸 | `768x1024` |
| 网格 | 6 列 x 8 行 |
| 单帧 | `128x128` |
| 行顺序 | 第 0-7 行按 `front/down -> down_right -> right -> up_right -> back/up -> up_left -> left -> down_left` |
| 每行动画 | 6 帧 walk 循环 |
| 游戏显示尺寸 | 当前运行时约 `52x52`，所以轮廓必须清晰，细节不要过碎。 |

方向定义按屏幕坐标理解：

| 行 | 方向 | 说明 |
| ---: | --- | --- |
| 0 | front/down | 面向屏幕下方，玩家向下移动时播放。 |
| 1 | down_right | 面向右下，玩家向右下移动时播放。 |
| 2 | right | 面向右侧，玩家向右移动时播放。 |
| 3 | up_right | 面向右上，玩家向右上移动时播放。 |
| 4 | back/up | 面向屏幕上方，玩家向上移动时播放。 |
| 5 | up_left | 面向左上，玩家向左上移动时播放。 |
| 6 | left | 面向左侧，玩家向左移动时播放。 |
| 7 | down_left | 面向左下，玩家向左下移动时播放。 |

玩家角色制作要求：

- 每行 6 帧必须是同一动作循环，不要混入攻击、受击、跳跃、大招动作。
- 不要把攻击火光、弹道、爆炸绘制进 walk sheet；当前游戏射击由运行时绘制。
- 正面、背面、侧面高度要一致，脚底线建议固定在 `y=116-122` 附近。
- 主体中心建议固定在 `x=64` 附近，允许动作摆臂轻微偏移，但躯干不能左右漂移。
- 头顶、脚底、肩宽每帧变化不能过大；动画可以动，但角色本体不能跳。
- 头像优先从正面第 1 帧裁切，命名为 `avatar_<角色id>.webp`。
- 选择页预览 GIF 用同一份 normalized sheet 生成，命名为 `preview_<角色id>_walk.gif`。

## 3. 普通怪兽规格

当前普通怪兽动画由 `EnemyAnimationSet` 读取，默认规格如下：

| 项目 | 规格 |
| --- | --- |
| 文件位置 | `assets/game/grass_game/images/guaishou/`，动物小怪放 `assets/game/grass_game/images/animals/runtime/` |
| 文件名 | `guaishou_<怪物id>_walk_8dir_sheet.png` 或 `animal_<id>_walk_sheet_runtime_128.png` |
| 图片尺寸 | `768x1024` |
| 网格 | 6 列 x 8 行 |
| 单帧 | `128x128` |
| 运行时使用帧 | 每个方向使用 6 帧 |
| 运行时使用行 | 8 行全部是 walk：front/down、down_right、right、up_right、back/up、up_left、left、down_left |

旧 4 方向源图不能直接运行时读取，必须先转换成 `768x1024 / 6列 x 8行 / 128x128` 的 walk 运行图。

普通怪兽行顺序：

| 行 | 方向/用途 | 说明 |
| ---: | --- | --- |
| 0 | front/down walk | 向屏幕下方。 |
| 1 | down_right walk | 向右下。 |
| 2 | right walk | 向右。 |
| 3 | up_right walk | 向右上。 |
| 4 | back/up walk | 向屏幕上方。 |
| 5 | up_left walk | 向左上。 |
| 6 | left walk | 向左。 |
| 7 | down_left walk | 向左下。 |

普通怪兽制作要求：

- 小怪同屏数量很多，轮廓要强，局部细节要少。
- 不能每帧改变整体大小；体型变化应控制在 5% 以内。
- 快速怪可以更小，但要保留明显颜色或剪影差异。
- 厚血怪可以更大，但不能占满 `128x128`，否则碰撞观感会比图像小。
- 不要带复杂投影、长尾焰、大范围光效，这些会影响同屏性能和识别。
- 如果素材只做 4 行，需要先转换成当前 8 行结构，不能直接接入。
- 当前怪兽不接攻击动画；攻击玩家时保持当前 walk 方向，不做额外攻击行切换。

## 4. Boss 规格

Boss 体积约为玩家 4 倍，当前运行时显示尺寸通常约 `104x104`；需要突出关卡最终 Boss 时可以在运行时配置更大的显示尺寸，但 spritesheet 单帧仍保持 `128x128`。

| 项目 | 规格 |
| --- | --- |
| 文件位置 | `assets/game/grass_game/images/guaishou/` |
| 8 方向行走命名 | `guaishou_<bossid>_walk_8dir_sheet.png` |
| 运行时图片尺寸 | `768x1024` |
| 运行时网格 | 6 列 x 8 行，单帧 `128x128` |
| 死亡图片 | 不再单独接入旧 `dead.png`；死亡表现由运行时特效或后续 death sheet 统一补充 |
| 行顺序 | 和普通怪兽一致，8 行全部是 walk：front/down、down_right、right、up_right、back/up、up_left、left、down_left |

Boss 制作要求：

- Boss 可以更细，但游戏中实际显示仍较小，必须先保证剪影和主色块清晰。
- 头、身体、武器或翅膀不能频繁跨越单元格边缘。
- 死亡图应保持同一角色、同一透视、同一比例，不要换成完全不同姿态或画风。
- Boss 不要自带红圈、警戒圈、UI 边框；这类提示由游戏运行时绘制。
- 当前不接 Boss 攻击动画；攻击玩家时保持当前 walk 方向，不做额外攻击行切换。

## 5. 锚点与裁切标准

每个 `128x128` 单元格都按下面标准验收：

| 检查项 | 合格标准 |
| --- | --- |
| 透明边界 | 单元格边缘不应有残留色块、棋盘格、截图背景。 |
| 脚底线 | 同一行所有帧脚底 y 坐标差异不超过 4px。 |
| 躯干中心 | 同一行所有帧主体中心 x 坐标差异不超过 6px。 |
| 头顶高度 | 同一行所有帧头顶 y 坐标差异不超过 8px。 |
| 角色比例 | 不同方向之间视觉身高差异不超过 10%。 |
| 动作幅度 | 可以摆臂、迈腿，但不能让整个人物在格子内横跳。 |

推荐安全框：

```text
128x128 cell
  x: 6-122    主要可用范围
  y: 4-122    主要可用范围
  x: 64       身体中心参考线
  y: 116-122  脚底参考线
```

如果原始素材主体位置不稳定，必须先做 normalized sheet：

1. 去除背景，只保留最大主体。
2. 按每帧主体真实透明边界裁切。
3. 重新粘贴到 `128x128` 单元格。
4. 水平居中，脚底对齐。
5. 再生成最终 spritesheet、头像和预览 GIF。

## 6. 命名规范

只使用英文小写、数字和下划线。

```text
player_02_walk_8dir_sheet.png
avatar_player_02.png
preview_player_02_walk.gif

animal_lamb_walk_sheet_runtime_128.png
animal_calf_walk_sheet_runtime_128.png
animal_rooster_walk_sheet_runtime_128.png
guaishou_black_armored_beetle_walk_8dir_sheet.png

guaishou_cyber_crocodile_boss_walk_8dir_sheet.png
```

不要使用：

```text
凹凸曼.png
人物 新版.png
player-final-copy-2.png
monster@2x.png
素材截图.jpeg
```

## 7. 交付清单

新增一个玩家角色至少交付：

- `player_<id>_walk_8dir_sheet.png`
- `avatar_<id>.png` 或 `avatar_<id>.webp`
- `preview_<id>_walk.gif`
- 原始源文件或可编辑文件，放设计归档，不直接进 Flutter assets。
- 授权说明。

新增一个普通怪兽至少交付：

- `guaishou_<id>_walk_8dir_sheet.png` 或 `animal_<id>_walk_sheet_runtime_128.png`
- 授权说明。
- 怪物定位说明：普通、快速、厚血、远程、特殊。

新增一个 Boss 至少交付：

- `guaishou_<bossid>_walk_8dir_sheet.png`
- 授权说明。
- Boss 定位说明：生命值、移动方式、技能提示是否需要运行时特效。

## 8. 接入前验收清单

接入代码前逐项检查：

- 图片尺寸是否符合对应规格。
- 是否为真透明 RGBA。
- 每个方向行数和顺序是否正确。
- 每行帧数是否满足运行时要求。
- 是否没有棋盘格、白边、水印、文字。
- 播放预览 GIF 是否没有闪烁、跳动、忽大忽小。
- 正面、背面、侧面的视觉比例是否一致。
- 游戏显示尺寸下是否能识别角色和怪兽类型。
- 同屏大量怪兽时颜色是否过乱或过暗。
- 是否已确认授权，并记录来源。

建议每次接入后至少做三件事：

```bash
flutter analyze
flutter test
flutter build apk --debug
```

涉及新角色或新怪兽时，还要真机进入战斗页面检查：

- 移动时方向是否正确。
- 静止时是否停在当前方向。
- 射击时人物是否不闪。
- 怪兽追踪玩家时是否不频繁切方向抖动。
- 掉血、血条、碰撞观感是否和图片大小一致。

## 9. 给设计师或 AI 的简短需求模板

```text
请按 Flutter + Flame 割草小游戏规格制作角色/怪兽 spritesheet。

视角：2D 轻俯视，方向统一。
背景：真透明 RGBA，不要棋盘格背景。
单帧：128x128。
整图：768x1024，6列x8行。
人物、普通怪、动物小怪、Boss 行顺序一致：第0行正面/向下，第1行右下，第2行右，第3行右上，第4行背面/向上，第5行左上，第6行左，第7行左下。
每行6帧walk，不混入攻击、受击、死亡或特效。
锚点：每帧脚底对齐，主体水平居中，不要在格子内跳动。
风格：轮廓清晰，适合手机小尺寸显示，不带文字、水印、UI圈、武器火光特效。
交付：PNG spritesheet + 预览GIF + 授权说明。
```
